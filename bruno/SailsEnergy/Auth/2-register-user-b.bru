meta {
  name: 2. Register User B
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "userB@test.com",
    "displayName": "userB",
    "password": "Password123!",
    "confirmPassword": "Password123!",
    "firstName": "User",
    "lastName": "Beta"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("userBId", res.body.userId);
  }
}
