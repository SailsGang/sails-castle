meta {
  name: 1. Create Gang
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/gangs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Test Gang",
    "description": "Our test gang for API testing"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("gangId", res.body.gangId);
  }
}
